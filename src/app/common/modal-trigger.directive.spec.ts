import { ModalTriggerDirective } from "./ModalTriggerDirective";

describe('ModalTriggerDirective', () => {
  it('should create an instance', () => {
    const directive = new ModalTriggerDirective();
    expect(directive).toBeTruthy();
  });
});
